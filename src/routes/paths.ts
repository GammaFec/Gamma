export enum PATHS {
    Splash = "/Splash",
    Home = "/home",
    Login = "/login",
    MovilAuth = "/movil-auth/:id",
    Onboarding = "/onboarding",
    Match = "/match",
    GiveUp = "/give-up",
    Messages = "/messages",
    Favorites = "/favorites",
    Config = "/configuration"
}
