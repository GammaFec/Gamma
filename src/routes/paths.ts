export enum PATHS {
    Splash = "/Splash",
    Home = "/home",
    Login = "/login",
    Messages = "/messages",
    MovilAuth = "/movil-auth/:id",
    Onboarding = "/onboarding",
    Match = "/match",
    GiveUp = "/give-up",
    Messages = "/messages",
    Favorites = "/favorites",
    Config = "/configuration"
}
