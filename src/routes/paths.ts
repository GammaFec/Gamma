export enum PATHS {
    Splash = "/splash",
    Home = "/home",
    Login = "/login",
    ChooseProfile = "/choose-profile",
    MovilAuth = "/movil-auth/:id",
    Messages = "/messages",
    Onboarding = "/onboarding"
}
